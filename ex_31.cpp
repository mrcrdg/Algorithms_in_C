#include <stdio.h>
#include <stdlib.h>

/*31.	Leia 10 números inteiros e armazene em um vetor v. Crie dois novos vetores v1 e v2. 
Copie os valores ímpares de v para v1, e os valores pares de v para v2.
Note que cada um dos vetores v1 e v2 têm no máximo 10 elementos, mas nem todos os elementos são utilizados.
No final escreva os elementos UTILIZADOS de v1 e v2.
*/

int main(){
	
	int vetor[10];
	int v1[10], v1[10];
	int i, nv1, nv2 = 0; // quantos numeros existem em cada vetor
	
 	//Ler os valores para vetor[10]
 	for(i = 0; i < 10; i++){
 		printf("Insira o valor %i/10: ", i+1);
 		scanf("%i", &vetor[i]);
 		printf("\n"); 			
 	}
 	 	
 	for(int i = 0; i < 10; i++){
 		if (v[i] % 2 == 0) {
 			v2[nv2] = v[i];
 			nv2++;
 		} else {
 	
 	
 	
 	
	}
		return 0;
}
